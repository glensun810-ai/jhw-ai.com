# 进化湾网站 — 设计规范

> 版本：v1.0  
> 日期：2026-07-01  
> 适用范围：jhw-ai.com 全站前端  

---

## 一、Emoji 使用规范

### 1.1 核心原则

**导航栏、页脚、按钮、CTA 等交互元素中，禁止使用 emoji。**

理由：emoji 在不同浏览器和操作系统上渲染差异大（Apple/Google/Microsoft/微信内置浏览器均不同），且整体气质偏 casual，与进化湾面向企业客户的品牌定位不符。

### 1.2 允许使用的场景

| 场景 | 是否允许 | 说明 |
|------|---------|------|
| **导航链接**（nav） | ❌ **禁止** | 例如：~~🏭 制造业智能体~~ → 应写为"制造经营大脑" |
| **页脚链接**（footer） | ❌ **禁止** | 例如：~~📊 仪表盘~~ → 应写为"仪表盘" |
| **按钮/CTA** | ❌ **禁止** | 例如：~~📞 联系我们~~ → 应写为"联系我们" |
| **功能标签**（section-label） | ⚠️ **酌情** | 仅用于区分大模块时可使用 1 个 emoji，如"🏭 标杆服务" → 但如果可以，建议用纯文字 |
| **卡片标题/内容** | ✅ **允许** | 如"📈 多签单"等五维能力标题中的 emoji，作为内容标识使用 |
| **数据指标/数据对比** | ✅ **允许** | 如"❌ 传统方式 / ✅ 制造经营大脑"中的 emoji |
| **表情符号（封闭区域）** | ✅ **允许** | 如💡、⚠️、✅ 等，在提示性文字中使用 |

### 1.3 检查清单

每次提交前对照：

- [ ] 所有 `<nav>` 中的 `<a>` 标签文本不含 emoji
- [ ] 所有 `<footer>` 中的 `<a>` 标签文本不含 emoji
- [ ] 所有 class 包含 `btn`、`cta`、`button` 的元素的文本不含 emoji
- [ ] 所有 `section-label` 优先使用纯文字，确需用 emoji 时不超过 1 个

---

## 二、品牌色调规范

### 2.1 主色调

| 用途 | 色值 | 备注 |
|------|------|------|
| 品牌主色（Primary） | `#0f4c81` | 深蓝——首屏、按钮、关键强调 |
| 品牌深色（Dark） | `#1a1a2e` / `#16213e` | 深色背景 section 使用 |
| 强调色（Accent） | `#4ecdc4` | 新功能模块、数据高亮、正向指标 |
| 警示色 | `#ff6b6b` | 数据下降、错误提示 |
| 背景色 | `#fafbfc` / `#f8f9fb` | 浅色 section 背景 |

### 2.2 文字色

| 用途 | 色值 | 备注 |
|------|------|------|
| 主标题 | `#1a1a2e` | 深色背景上用 `#fff` |
| 正文 | `#2c3e50` | 深色背景上用 `rgba(255,255,255,0.7)` |
| 次要文字 | `#666` / `#555` | 深色背景上用 `rgba(255,255,255,0.5)` |
| 辅助说明 | `#999` | 深色背景上用 `rgba(255,255,255,0.35)` |

### 2.3 背景色应用规则

- 浅色 section（白色 / `#fafbfc`）：用于"核心服务"、"关于我们"、"新闻"等常规内容
- 深色 section（`#0a0a1a` 渐变至 `#16213e`）：用于重点推广模块（如"制造经营大脑"），形成视觉区隔
- 深色 section 之间不应直接拼接浅色 section——中间需要渐变过渡或分割线

---

## 三、排版规范

### 3.1 字体

```css
font-family: 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', -apple-system, sans-serif;
```

### 3.2 标题层级

| 层级 | 字号 | 字重 | 场景 |
|------|------|------|------|
| Hero H1 | `clamp(2em, 4.5vw, 3em)` | 700 | 首页首屏大标题 |
| Section H2 | `clamp(1.6em, 3.5vw, 2.4em)` | 700 | 各大模块主标题 |
| Subsection H3 | `clamp(1.2em, 2.5vw, 1.6em)` | 600 | 模块内子标题 |
| Card H4 | `1em-1.1em` | 600 | 卡片标题 |
| 正文 | `0.9em-1em` | 400 | 内容段落 |

---

## 四、交互规范

### 4.1 按钮

| 类型 | 样式 | 用途 |
|------|------|------|
| 主 CTA | 渐变背景（`#4ecdc4` → `#0f4c81`）+ 白色文字 | 最重要的行动号召 |
| 次 CTA | `#0f4c81` 实色背景 + 白色文字 | 表单提交、跳转 |
| 三级 | 透明边框 + 有色文字 | 次要操作"了解更多" |

### 4.2 Tab 切换

- 当前活跃 tab：背景色 + 有色文字
- 非活跃 tab：透明背景 + 半透明白色文字
- 切换动效：即时切换（无需过渡动画），减少感知延迟

### 4.3 卡片悬停

- 浅色卡片：`box-shadow` 增强 + `translateY(-2px)`
- 深色卡片：背景透明度增加 + `translateY(-2px)`

---

## 五、内容语气规范

### 5.1 面向客户的语言原则

| 原则 | 说明 | 正确示例 | 错误示例 |
|------|------|---------|---------|
| **用"你"不用"您"** | 保持商务但不过度生硬 | "帮你算清" | "为您提供专业的" |
| **少用形容词，多用数据** | "3-5 天→30 分钟" 比"大幅提升"有力 | "报价 3-5 天→30 分钟" | "极速报价，大幅提升效率" |
| **先给价值，再讲功能** | 客户买的是"多签单"不是"智能报价助手" | "报价又快又准" | "内置智能报价算法引擎" |
| **避免自夸式表述** | "领先""顶级""首创"等词降低可信度 | "根据真实客户数据" | "行业领先的AI解决方案" |
| **不说"代替人"，说"帮人"** | 制造业老板怕被替代 | "AI 帮你沉淀经验" | "AI 代替人工报价" |

### 5.2 产品命名规范

- 产品名称：**制造经营大脑**
- 副标题格式：五维动词式表达，如"帮老板多签单·算清账·留住人·快交付·避风险"
- 避免命名：~~"制造业商务智能体"~~、"智能报价系统"（太技术化）

---

## 六、SEO 元数据规范

### 6.1 Title 格式

```
主关键词 | 次关键词 | 进化湾®
```

示例：
```
制造经营大脑 | 制造业 AI 经营系统 | 进化湾®
进化湾 | AI 创业孵化·营销增长·算力服务器 — 深圳 AI 产业服务平台
```

### 6.2 Description 格式

- 前 80 个字符包含核心关键词和价值主张
- 包含具体数据（天数、百分比、金额）
- 包含联系方式（电话）

---

## 七、设计检查清单

每次新增/修改页面后：

- [ ] 导航（nav）无 emoji
- [ ] 页脚（footer）无 emoji
- [ ] 按钮/CTA 文案无 emoji
- [ ] section 背景色深浅搭配合理，没有两块深色直接拼接
- [ ] 所有数据对照用了「优化前 → 优化后」的格式
- [ ] 移动端断点（900px / 640px）下所有内容可读
- [ ] section 标签开闭平衡（`<section>` = `</section>`）
- [ ] 页面 Title 和 Meta Description 已填写
- [ ] 结构化数据（JSON-LD）已在 Product/Service 页面添加

---

*本规范将随网站迭代持续更新。*
