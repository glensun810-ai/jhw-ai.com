# 进化湾后台管理系统 — 功能优化规划方案

> 文档版本: v1.0  
> 评估日期: 2026-06-04  
> 评估范围: 当前后台管理系统功能完整性、优化空间、新增功能可行性

---

## 一、当前后台功能全景

### 现有功能清单

| 功能模块 | 子功能 | 状态 | 说明 |
|---|---|---|---|
| **登录认证** | 用户名/密码登录 | ✅ 已有 | JWT Token |
|  | Token 过期自动跳转登录 | ✅ 已有 | |
| **仪表盘** | 线索统计卡片（总/新/联系中/已转化） | ✅ 已有 | |
|  | 每日提交趋势图（30天） | ✅ 已有 | Chart.js 折线图 |
|  | 服务类型分布图 | ✅ 已有 | Chart.js 饼图 |
|  | 最近提交列表 | ✅ 已有 | 最近5条 |
| **线索管理** | 线索列表+分页 | ✅ 已有 | 每页20条 |
|  | 搜索（姓名/公司/电话/微信/需求） | ✅ 已有 | |
|  | 状态筛选 | ✅ 已有 | |
|  | 排序 | ✅ 已有 | 列头点击排序 |
|  | 详情模态框编辑 | ✅ 已有 | |
|  | 快速状态变更 | ✅ 已有 | |
|  | 删除线索（含确认） | ✅ 已有 | |
|  | CSV 导出（含筛选条件） | ✅ 已有 | |
| **访问统计** | 累计访问量 | ⚠️ 待完善 | API 已部署，管理后台面板缺失 |
|  | 独立访客数 | ⚠️ 待完善 | 同上 |
|  | 今日访问数 | ⚠️ 待完善 | 同上 |
|  | 14天趋势图 | ⚠️ 待完善 | 同上 |
|  | 热门页面排行 | ⚠️ 待完善 | 同上 |
| **网站前端** | 百度统计代码 | ✅ 已有 | 需配置真实 ID 后生效 |
|  | Nginx 日志定时解析 | ✅ 已有 | cron 每5分钟 |

---

## 二、待优化问题

### 2.1 现有功能的完善

| 问题 | 优先级 | 说明 |
|---|---|---|
| 访问统计面板未接入 | **P0** | API 已部署、数据显示面板代码已写到 HTML 但未接入 JS 加载流程 |
| 线索字段中超链接不可点击 | P2 | 电话、邮箱等在模态框中为纯文本，不可一键拨打/发送 |
| 无操作日志 | P2 | 谁在什么时候修改了什么线索，无记录 |

### 2.2 新功能需求分析

按优先级排列：

---

## 三、新增功能详细评估

### P0 — 必须做

#### 1. 网站访问统计面板接入（功能完善）

| 维度 | 评估 |
|---|---|
| **必要性** | ★★★★★ — 网站运营的基础数据。没有访问统计=不知道网站效果 |
| **当前状态** | API 已工作（`/admin/api/analytics` 返回数据），管理后台 HTML 面板代码已存在但 JS 加载流程未正确启动 |
| **工作量** | 极小（约 30 分钟）——修复 JS 调用链路即可 |
| **价值** | 极高 — 可直接查看累计访问/今日访问/趋势/热门页面 |

#### 2. 线索数据概览 — 今日新增线索数

| 维度 | 评估 |
|---|---|
| **必要性** | ★★★★☆ — 仪表盘只有累计数据，没有"今天有多少新线索"的即时感 |
| **当前状态** | 可以从后端 `/admin/api/stats` 获取今日数据 |
| **工作量** | 小（约 1 小时）——后端加一个 `today_new` 字段，前端卡片展示 |
| **价值** | 高 — 让运营人员快速感知当日转化效果 |

---

### P1 — 建议做

#### 3. 服务类型转化漏斗

| 维度 | 评估 |
|---|---|
| **必要性** | ★★★★☆ — 现有服务类型饼图仅显示分布，不显示"哪种服务的提交转化率更高" |
| **方案** | 在现有饼图卡片下方增加转化率柱状图：每个服务的"新提交→联系中→已转化"转化率 |
| **工作量** | 中（约 3 小时）——后端 SQL 查询各服务各状态计数、前端 Chart.js 柱状图 |
| **价值** | 高 — 帮助优化服务结构和营销资源分配 |

#### 4. 线索详情中嵌入时间线

| 维度 | 评估 |
|---|---|
| **必要性** | ★★★☆☆ — 当前无法查看线索状态变更历史 |
| **方案** | 在详情模态框底部的"快速操作"区域上方加一个时间线组件，显示每次状态变更时间 |
| **前置条件** | 需新建 `lead_logs` 表记录操作日志 |
| **工作量** | 中（约 4 小时）——新表+后端 API+前端时间线组件 |
| **价值** | 中高 — 方便运营了解线索跟进节奏 |

#### 5. CSV 导出增加日期筛选

| 维度 | 评估 |
|---|---|
| **必要性** | ★★★☆☆ — 当前导出全部线索，无法按时间范围 |
| **方案** | 工具栏增加"开始日期""结束日期"两个日期输入框，导出时加入筛选 |
| **工作量** | 小（约 1.5 小时）——前端加日期选择器+后端 API 加日期参数 |
| **价值** | 中 — 有报表需求的用户更友好 |

#### 6. 响应式布局增强 — 线索表格手机端卡片化

| 维度 | 评估 |
|---|---|
| **必要性** | ★★★☆☆ — 当前表格列隐藏策略在极小屏上仍显拥挤 |
| **方案** | 480px 以下将表格行转为卡片式布局，每行显示为独立信息卡片 |
| **工作量** | 中（约 2 小时）——CSS 媒体查询+JS 渲染模式切换 |
| **价值** | 中 — 提升手机端易用性 |

---

### P2 — 值得做

#### 7. 线索来源分析

| 维度 | 评估 |
|---|---|
| **必要性** | ★★☆☆☆ — 线索表已有 `referrer` 和 `form_source` 字段，未利用 |
| **方案** | 仪表盘增加"线索来源分布"图表 |
| **工作量** | 小（约 2 小时）——后端 SQL 分组统计+前端饼图 |
| **价值** | 中 — 了解哪些渠道带来的线索最多 |

#### 8. 导出格式扩展（Excel 替代 CSV）

| 维度 | 评估 |
|---|---|
| **必要性** | ★★☆☆☆ — CSV 通用性强但无格式，Excel 更友好 |
| **方案** | 增加 Excel 导出选项（.xlsx），使用 openpyxl |
| **前置条件** | 服务器安装 openpyxl |
| **工作量** | 小（约 2 小时）——后端增加导出函数 |
| **价值** | 低中 — 运营人员更方便 |

#### 9. 暗色模式

| 维度 | 评估 |
|---|---|
| **必要性** | ★★☆☆☆ — 后台管理系统暗色模式对 B2B 场景非必需 |
| **方案** | CSS 变量切换 |
| **工作量** | 中（约 3 小时）—— CSS 变量两套 + JS 切换按钮 |
| **价值** | 低 — 提升体验但非功能必需 |

---

## 四、分阶段实施路径

### 第一阶段（本周 — 极小投入，极大回报）

| 功能 | 工作量 | 价值 |
|---|---|---|
| **访问统计面板接入** | 30 分钟 | 极高 |
| **今日新增线索数** | 1 小时 | 高 |

### 第二阶段（下月 — 中等投入）

| 功能 | 工作量 | 价值 |
|---|---|---|
| 服务类型转化漏斗 | 3 小时 | 高 |
| 线索时间线 | 4 小时 | 中高 |
| CSV 日期筛选 | 1.5 小时 | 中 |
| 手机端卡片化 | 2 小时 | 中 |

### 第三阶段（按需）

| 功能 | 工作量 | 价值 |
|---|---|---|
| 线索来源分析 | 2 小时 | 中 |
| Excel 导出 | 2 小时 | 低中 |
| 暗色模式 | 3 小时 | 低 |

---

## 五、架构建议

### 5.1 新增数据库表

```sql
-- 操作日志表（用于线索时间线）
CREATE TABLE lead_logs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    lead_id INTEGER NOT NULL,
    action TEXT NOT NULL,          -- 'create', 'update_status', 'update_field', 'delete'
    field_name TEXT,               -- 变更的字段名
    old_value TEXT,                -- 旧值
    new_value TEXT,                -- 新值
    operator TEXT,                 -- 操作人
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (lead_id) REFERENCES leads(id)
);
```

### 5.2 API 扩展路线

```
当前 API 完善阶段（P0）：
  /admin/api/analytics  ← 已有，需前端接入
  /admin/api/stats      ← 增加 today_new 字段

第二阶段（P1）：
  /admin/api/stats      ← 增加 service_funnel 字段
  /admin/api/leads/<id>/logs  ← 新增，线索操作日志
  /admin/api/export     ← 增加 date_from/date_to 参数

第三阶段（P2）：
  /admin/api/stats      ← 增加 source_distribution 字段
```

### 5.3 安全隐患（当前来看是否要做）

| 风险 | 级别 | 建议 |
|---|---|---|
| 导出 API 的 token 通过 URL 参数传递 | **中** | 改为 Header 传递（已有 Bearer 方案，导出时需统一） |
| 无登录失败锁定机制 | 低 | B2B 内部系统，当前阶段可不做 |
| Token 有效期固定 12 小时 | 低 | 当前可接受 |

---

## 六、结论

### 立即可做（30 分钟~1 小时）

1. **接入访问统计面板** — API 已通，只差前端 JS 接入
2. **仪表盘增加今日新增线索数** — 后端 SQL 已可查

### 建议近期做（8~10 小时）

3. 服务转化漏斗
4. 线索时间线
5. CSV 日期筛选
6. 手机端卡片化

### 按需再做

7. 线索来源分析
8. Excel 导出
9. 暗色模式

---

*文档结束*
